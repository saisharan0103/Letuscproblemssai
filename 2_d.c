#include<stdio.h>
#include<math.h>
int main(){

    float A,Z,R,X,a,b,c,d,g,v,x,y;
    // a
    Z = (x + 3) * x * x * x / ((y - 4) * (y + 5));
    // b
    R = 2 * v + 6.22 * (c + d) / g + v;
    // c
    A = (7.7 * b * (x * y + a) / c - 0.8 + 2 * b) / ((x + a) * (1 / y));
    // d
    X = (( (12*(pow(x,3))/(4*x)) + (8*(pow(x,2))/(4*x)) + (x/(8*x)) + (8/(8*x)) ));

    return 0;
}