#include<stdio.h>
int main( )
{
    // a question
    // float a = 12.25, b = 12.52 ;
    // if ( a = b )   /* We get  error when we are comparing a and b, we have to use " == " */
    // printf ( "a and b are equal\n" ) ;
    // return 0 ;

    // a answer
    float a = 12.25, b = 12.52 ;
    if ( a == b )
        printf ( "a and b are equal\n" ) ;
    return 0 ;

}
